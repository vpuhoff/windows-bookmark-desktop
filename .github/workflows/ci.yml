# Проверка PR и ветки main: тесты и сборка расширения (без релиза)
name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read
  actions: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-node@v6
        with:
          node-version: "20"
          cache: npm

      - name: Install
        run: npm ci

      - name: Test
        run: npm run test

      - name: Build extension
        run: npm run build:ext

      - name: Upload extension artifact
        uses: actions/upload-artifact@v7
        with:
          name: extension-dist-${{ github.sha }}
          path: dist/
          retention-days: 14
